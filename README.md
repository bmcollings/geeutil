# Geeutil
Series of tools for working with public optical satellite image collections with Google Earth Engine Python API. This includes tools to download imagery locally (Note - max request per image is 32MB, and max grid dimension is 10000). 

## Installation 

`pip install geeutil`

Some functions use gdal which incudes extra install steps for full functionality - see [here](https://pypi.org/project/GDAL/).